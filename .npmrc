# NPM Configuration for Manylla

# Note: audit-level is NOT set here
# QUAL deployment uses --audit-level=none in deploy-qual.sh
# STAGE/PROD will use default (strict) audit levels
# See SECURITY_WORKAROUNDS.md for details on React Native CLI vulnerability

# Package installation settings
save-exact=false
package-lock=true
