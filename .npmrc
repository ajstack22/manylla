# NPM Configuration for Manylla

# Suppress critical audit level for deployment
# Reason: React Native CLI v15 has dev-only vulnerability (CVE-2025-11953)
# Does not affect deployed applications (web bundles or mobile builds)
# See docs/TECH_DEBT.md section 0a for full details
# TODO: Remove after upgrading to React Native 0.81+ with CLI v20+
audit-level=high

# Package installation settings
save-exact=false
package-lock=true
