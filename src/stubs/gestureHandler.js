/**
 * Stub file for react-native-gesture-handler web compatibility
 * Provides empty implementations for native gesture handler modules
 * that don't exist in web builds.
 */

// Export empty objects/functions for any imports that might be needed
export const NativeViewGestureHandler = {};
export const createNativeWrapper = () => null;
export const gestureObjects = {};
export const utils = {};
export const Gestures = {};
export const nativeGesture = {};

// Default export as an empty object
const gestureHandler = {};
export default gestureHandler;
